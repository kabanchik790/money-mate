# MoneyMate Telegram Mini App

Полностью переписанная версия мобильного приложения учета финансов в формате Telegram WebApp. Мини-приложение повторяет функциональность Flutter-версии (доходы, расходы, список желаний, сброс данных и темы) и использует **Supabase** для хранения данных в облаке.

## Технологии

- [Vite](https://vitejs.dev/) + React 18 + TypeScript
- [Supabase](https://supabase.com/) (PostgreSQL в облаке)
- [Telegram WebApp SDK](https://core.telegram.org/bots/webapps)
- Zustand для клиентского состояния

## Быстрый старт

1. Установите зависимости:
```bash
cd telegram-mini-app
npm install
```

2. Настройте Supabase (см. [SUPABASE_SETUP.md](./SUPABASE_SETUP.md)):
   - Создайте проект в Supabase
   - Создайте таблицы через SQL Editor
   - Создайте файл `.env` с ключами API

3. Запустите проект:
```bash
npm run dev
```

**Важно**: Для работы приложения необходимо настроить Supabase. Подробная инструкция находится в файле [SUPABASE_SETUP.md](./SUPABASE_SETUP.md).

## Деплой в Telegram Mini App

После настройки Supabase и локального тестирования, вы можете задеплоить приложение в Telegram Mini App. Подробная инструкция находится в файле [DEPLOYMENT.md](./DEPLOYMENT.md).

Кратко:
1. Задеплойте проект на Vercel/Netlify/GitHub Pages
2. Создайте бота через [@BotFather](https://t.me/BotFather)
3. Настройте Mini App в BotFather
4. Готово! 🎉

## Скрипты

- `npm run dev` — локальная разработка (Vite dev server)
- `npm run build` — production билд
- `npm run preview` — предпросмотр собранной версии

## Структура

- `src/components` — UI-компоненты (листы, формы, модалки, карточки)
- `src/pages` — страницы: главная, желания, настройки
- `src/store` — Zustand-стор с бизнес-логикой
- `src/hooks` — работа с Telegram темой и пользователем
- `src/lib` — утилиты (Supabase-клиент, форматирование чисел)

## Telegram интеграция

Мини-приложение автоматически подхватывает цвета и пользователя из Telegram WebApp API, расширяет окно (`WebApp.expand()`) и реагирует на изменение темы (`themeChanged`). При необходимости пользователь может вручную выбрать светлую/темную тему в настройках — выбор сохраняется в `localStorage`.

## Лицензия

Проект создан в учебных целях на основе исходного приложения MoneyMate.

