# Гайд по деплою Telegram Mini App

Этот гайд поможет вам задеплоить проект Money Mate в Telegram Mini App.

## Содержание

1. [Подготовка проекта](#1-подготовка-проекта)
2. [Выбор хостинга](#2-выбор-хостинга)
3. [Деплой на Vercel (рекомендуется)](#3-деплой-на-vercel-рекомендуется)
4. [Деплой на Netlify](#4-деплой-на-netlify)
5. [Деплой на GitHub Pages](#5-деплой-на-github-pages)
6. [Настройка Telegram Bot](#6-настройка-telegram-bot)
7. [Подключение Mini App к боту](#7-подключение-mini-app-к-боту)
8. [Проверка работы](#8-проверка-работы)

---

## 1. Подготовка проекта

### 1.1. Настройте переменные окружения

Убедитесь, что файл `.env` содержит реальные значения:

```env
VITE_SUPABASE_URL=https://ваш-проект.supabase.co
VITE_SUPABASE_ANON_KEY=ваш-реальный-ключ
```

### 1.2. Проверьте сборку локально

```bash
cd telegram-mini-app
npm run build
```

Если сборка прошла успешно, в папке `dist` появятся готовые файлы.

### 1.3. Проверьте preview

```bash
npm run preview
```

Откройте `http://localhost:4173` в браузере и убедитесь, что всё работает.

---

## 2. Выбор хостинга

Для Telegram Mini App подходят следующие бесплатные хостинги:

| Хостинг | Плюсы | Минусы |
|---------|-------|--------|
| **Vercel** | Автодеплой из Git, быстрый, простой | Требует Git репозиторий |
| **Netlify** | Автодеплой, хорошая документация | Может быть медленнее |
| **GitHub Pages** | Бесплатно, интеграция с GitHub | Нужен публичный репозиторий |
| **Cloudflare Pages** | Очень быстрый, бесплатный | Меньше функций |

**Рекомендация**: Используйте **Vercel** — самый простой и быстрый вариант.

---

## 3. Деплой на Vercel (рекомендуется)

### Шаг 1: Подготовьте репозиторий

1. Создайте репозиторий на GitHub (если ещё нет):
   ```bash
   cd '/Users/andrewsmiriagin/учеба/4 курс/Money Mate (курсовой проект)'
   git init
   git add .
   git commit -m "Initial commit"
   ```

2. Создайте репозиторий на GitHub и загрузите код:
   ```bash
   git remote add origin https://github.com/ваш-username/money-mate.git
   git push -u origin main
   ```

### Шаг 2: Настройте переменные окружения в Git

**Важно**: Не коммитьте `.env` файл! Вместо этого используйте переменные окружения на хостинге.

Убедитесь, что `.env` в `.gitignore` (уже добавлен).

### Шаг 3: Деплой на Vercel

1. Зайдите на [vercel.com](https://vercel.com)
2. Войдите через GitHub
3. Нажмите **"Add New Project"**
4. Выберите ваш репозиторий `money-mate`
5. Настройте проект:
   - **Framework Preset**: Vite
   - **Root Directory**: `money_mate/telegram-mini-app`
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
6. Добавьте переменные окружения:
   - Нажмите **"Environment Variables"**
   - Добавьте:
     - `VITE_SUPABASE_URL` = ваш URL
     - `VITE_SUPABASE_ANON_KEY` = ваш ключ
7. Нажмите **"Deploy"**

### Шаг 4: Получите URL

После деплоя Vercel даст вам URL вида:
```
https://money-mate-xxxxx.vercel.app
```

Сохраните этот URL — он понадобится для настройки Telegram Bot.

---

## 4. Деплой на Netlify

### Шаг 1: Подготовьте репозиторий (см. раздел 3.1)

### Шаг 2: Деплой

1. Зайдите на [netlify.com](https://netlify.com)
2. Войдите через GitHub
3. Нажмите **"Add new site"** → **"Import an existing project"**
4. Выберите репозиторий
5. Настройте:
   - **Base directory**: `money_mate/telegram-mini-app`
   - **Build command**: `npm run build`
   - **Publish directory**: `dist`
6. Добавьте переменные окружения:
   - **Site settings** → **Environment variables**
   - Добавьте `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY`
7. Нажмите **"Deploy site"**

URL будет вида: `https://your-app-name.netlify.app`

---

## 5. Деплой на GitHub Pages

### Шаг 1: Настройте vite.config.ts

Убедитесь, что в `vite.config.ts` указан правильный `base`:

```typescript
export default defineConfig({
  base: '/money-mate/', // или '/', если репозиторий называется money-mate
  // ... остальные настройки
});
```

### Шаг 2: Создайте GitHub Actions

Создайте файл `.github/workflows/deploy.yml`:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          
      - name: Install dependencies
        run: npm ci
        working-directory: ./money_mate/telegram-mini-app
        
      - name: Build
        run: npm run build
        working-directory: ./money_mate/telegram-mini-app
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
          
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./money_mate/telegram-mini-app/dist
```

### Шаг 3: Настройте Secrets

1. В репозитории GitHub: **Settings** → **Secrets and variables** → **Actions**
2. Добавьте:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`

### Шаг 4: Включите GitHub Pages

1. **Settings** → **Pages**
2. **Source**: выберите `gh-pages` branch
3. URL будет: `https://ваш-username.github.io/money-mate/`

---

## 6. Настройка Telegram Bot

### Шаг 1: Создайте бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Придумайте имя бота (например: "Money Mate")
   - Придумайте username (например: `moneymate_bot`)
4. Сохраните **токен бота** (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Шаг 2: Настройте Mini App

1. Отправьте BotFather команду `/newapp`
2. Выберите вашего бота из списка
3. Заполните форму:
   - **Title**: Money Mate
   - **Short name**: moneymate (без пробелов, только латиница)
   - **Description**: Учет личных финансов
   - **Photo**: загрузите иконку (512x512px, квадратная)
   - **GIF**: опционально, можно пропустить
   - **Web App URL**: вставьте URL вашего задеплоенного приложения
     - Для Vercel: `https://money-mate-xxxxx.vercel.app`
     - Для Netlify: `https://your-app.netlify.app`
     - Для GitHub Pages: `https://username.github.io/money-mate/`
   - **Short name**: подтвердите

4. BotFather даст вам ссылку вида: `https://t.me/ваш_бот/moneymate`

### Шаг 3: Настройте меню бота (опционально)

Отправьте BotFather команду `/setmenubutton`:
1. Выберите вашего бота
2. Нажмите на кнопку "Mini App" в меню бота

Или используйте команду:
```
/setmenubutton
@ваш_бот
Mini App|https://ваш-url.vercel.app
```

---

## 7. Подключение Mini App к боту

### Вариант 1: Через кнопку меню (автоматически)

После настройки в BotFather, кнопка Mini App появится в меню бота автоматически.

### Вариант 2: Через команду /start

Можно добавить обработчик команды `/start`, который будет открывать Mini App.

Для этого нужно создать простой backend (опционально), или просто использовать ссылку:
```
https://t.me/ваш_бот?startapp=moneymate
```

### Вариант 3: Inline кнопка

Создайте кнопку в боте, которая открывает Mini App. Это требует backend, но можно использовать готовые решения.

---

## 8. Проверка работы

### Шаг 1: Откройте Mini App

1. Найдите вашего бота в Telegram
2. Нажмите на кнопку "Mini App" в меню
3. Или перейдите по ссылке: `https://t.me/ваш_бот/moneymate`

### Шаг 2: Проверьте функциональность

- ✅ Приложение открывается
- ✅ Отображается интерфейс
- ✅ Можно добавить операцию
- ✅ Можно добавить желание
- ✅ Данные сохраняются в Supabase
- ✅ Тема подстраивается под Telegram

### Шаг 3: Проверьте консоль

1. Откройте DevTools в браузере (если тестируете через веб-версию)
2. Или используйте [Telegram Web](https://web.telegram.org) для тестирования
3. Проверьте, нет ли ошибок в консоли

### Шаг 4: Проверьте Supabase

1. Зайдите в Supabase Dashboard
2. Откройте **Table Editor**
3. Проверьте, что данные сохраняются в таблицах `money` и `shop`

---

## Устранение проблем

### Ошибка "Failed to fetch" или CORS

- Убедитесь, что Supabase URL правильный
- Проверьте, что RLS политики настроены
- Проверьте, что `anon` ключ правильный

### Mini App не открывается

- Проверьте, что URL правильный и доступен (откройте в браузере)
- Убедитесь, что URL начинается с `https://` (не `http://`)
- Проверьте настройки в BotFather

### Данные не сохраняются

- Проверьте переменные окружения на хостинге
- Проверьте консоль браузера на ошибки
- Убедитесь, что Telegram User ID определяется

### Белый экран при открытии

- Проверьте, что сборка прошла успешно
- Проверьте консоль на ошибки JavaScript
- Убедитесь, что все зависимости установлены

### Ошибка "Telegram User ID not found"

- Убедитесь, что приложение открыто внутри Telegram
- Проверьте, что `WebApp.initDataUnsafe?.user?.id` доступен
- В режиме разработки можно добавить тестовый ID

---

## Полезные ссылки

- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Telegram Mini Apps](https://core.telegram.org/bots/webapps)
- [Vercel Documentation](https://vercel.com/docs)
- [Netlify Documentation](https://docs.netlify.com)
- [GitHub Pages](https://pages.github.com)

---

## Дополнительные настройки

### Настройка домена (опционально)

Если хотите использовать свой домен:
1. Купите домен
2. Настройте DNS записи на хостинге
3. Добавьте домен в настройках Vercel/Netlify
4. Обновите URL в BotFather

### HTTPS обязателен

Telegram Mini Apps требуют HTTPS. Все перечисленные хостинги предоставляют HTTPS автоматически.

### Обновление приложения

После каждого изменения в коде:
1. Закоммитьте изменения
2. Запушьте в Git
3. Хостинг автоматически задеплоит новую версию
4. Обновите страницу в Telegram

---

**Готово!** Ваше приложение теперь доступно как Telegram Mini App. 🎉

